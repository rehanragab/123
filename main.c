#include <stdio.h>
#include <stdlib.h>
#include"add.h"

int main()
{
    int addd=0;
    int x,y;

   // scanf("%d",x);
   // scanf("%d",y);
    addd= add(10,20);
    printf("%d",addd);
    return 0;
}
